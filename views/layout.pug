doctype html
html
  head
      block head
        title=title
        link(rel='stylesheet' href='/stylesheets/main.css')
  body
    nav#navbar
      a(href='/' id="homeButton")
        img(id="homeButtonImage" src="../images/JSJungle.svg", alt="sdfs")
      input(id="searchBar" type="search" placeholder="Search" name='searchbar')
      if true
        a(href='/users/1' id="profileButton") User's Name
        //- /users/#{user.id}
      else
        a(href='/login' id="logInButton") Log In
    block content
